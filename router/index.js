import RouteConfig from '../routes/config';
import _noop from 'lodash/noop';

// const routes = (RouteConfig.appRoutes || []).map ( (route = {}, index) => {
//     const { name, handler, http = {} } = route;
//     const { req, res } = http;
//     return handler.bind(undefined, req, res);
// } )

// function  invokeRoutes(app = _noop) {

// }